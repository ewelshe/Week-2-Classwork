void setup() {
  size(200, 200);
  background(255);
}

void draw() {
  background(255);
  rect(mouseX-10, mouseY -10 ,20, 20);
  line(mouseX -10, mouseY -10, mouseX, mouseY-20);
  line(mouseX , mouseY -20, mouseX +10, mouseY-10);
}
